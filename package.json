{
  "name": "uzbek-ai-callcenter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node create_icons.js && node generate_zip_bundle.js",
    "build": "npm run prebuild && vite build && node bundle_single_file.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jszip": "^3.10.1",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "lucide": "^0.439.0",
    "msedge-tts": "^2.0.7"
  }
}
